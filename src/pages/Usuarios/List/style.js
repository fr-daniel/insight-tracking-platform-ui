import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    width: '100%',
  },
  container: {
    maxHeight: 450,
  },
  margin: {
    margin: '5px',
  },
  title: {
    disply: 'block',
    padding: '10px'
  }
});

export default useStyles;
