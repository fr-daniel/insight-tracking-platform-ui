import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  table: {
    minWidth: 650,
  },
  padding: {
    padding: '10px',
    marginTop: '20px'
  }
});

export default useStyles;
